[run]
branch = True
source = app
omit =
    app/main.py
    app/db/*
    migrations/*
    # Exclude integration-heavy HTTP and orchestration layers from
    # unit-test coverage gate; these are validated in dedicated e2e flows.
    app/api/routes/*
    app/services/*
